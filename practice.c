#include<stdio.h>
//Calculating area of square
int main() {
  int side;
printf("Enter side value: ");
scanf("%d ", &side);
int area = side*side;
printf("Area of the square is: %d\n ", area);
return 0;
}
